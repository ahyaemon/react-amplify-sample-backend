name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch: []

jobs:
  push-ecr:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: hello world
        run: echo hello, world
